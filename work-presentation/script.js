//**
// .map & .filter()  ->

// map()
// Crea un nuovo array trasformando ogni elemento dell’array originale secondo una funzione.

// filter()
// Restituisce un nuovo array con solo gli elementi che soddisfano una condizione.

 //*

 const number = [1, 2, 3, 4, 5, 6, 7];

 const num = number.map(n => n * 2);
 console.log(num);